---
phase: 01-core-processing-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/processor.py
  - tests/test_processor.py
  - tests/fixtures/sample.xlsx
  - requirements.txt
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "판매자 고유코드 + 마켓명 조합 기준으로 중복 행이 제거된다"
    - "동일 집계 키의 주문수량이 숫자로 합산된다"
    - "각 집계 키에 최근 주문일 기준 상품명이 표시된다"
    - "각 집계 키에 최근 주문일이 표시된다"
    - "비정상 데이터(빈 주문수량, 빈 고유코드/마켓명)가 제외되고 건너뛴 건수가 요약에 포함된다"
  artifacts:
    - path: "src/processor.py"
      provides: "데이터 처리 모듈 (process_sales_data 함수)"
      exports: ["process_sales_data"]
    - path: "tests/test_processor.py"
      provides: "처리 로직 단위 테스트"
      min_lines: 50
    - path: "tests/fixtures/sample.xlsx"
      provides: "테스트용 엑셀 파일"
    - path: "requirements.txt"
      provides: "Python 의존성 목록"
  key_links:
    - from: "src/processor.py"
      to: "pandas.read_excel"
      via: "openpyxl engine"
      pattern: "pd\\.read_excel.*engine.*openpyxl"
    - from: "src/processor.py"
      to: "pandas.DataFrame.groupby"
      via: "집계 키 기준 그룹핑"
      pattern: "groupby"
    - from: "tests/test_processor.py"
      to: "src/processor.py"
      via: "import process_sales_data"
      pattern: "from src\\.processor import"
---

<objective>
pandas 기반 판매 데이터 처리 모듈을 TDD로 구현한다. 엑셀 파일을 읽어 판매자 고유코드 + 마켓명 조합으로 중복을 제거하고, 주문수량을 합산하며, 최근 주문일과 해당 상품명을 반환한다.

Purpose: Phase 2(Web Interface)에서 호출할 핵심 데이터 처리 엔진 구축
Output: process_sales_data() 함수 + 테스트 스위트 + 테스트 픽스처
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-processing-pipeline/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 테스트 픽스처 생성 및 실패 테스트 작성 (RED)</name>
  <files>tests/__init__.py, tests/fixtures/sample.xlsx, tests/test_processor.py, src/__init__.py, requirements.txt</files>
  <read_first>
    .planning/phases/01-core-processing-pipeline/01-CONTEXT.md
    .planning/REQUIREMENTS.md
    CLAUDE.md
  </read_first>
  <behavior>
    - Test 1 (DATA-01, D-01): 동일 고유코드+마켓명 행이 하나로 합쳐진다. 입력: 고유코드 "ABC001"+마켓 "스마트스토어" 행 3개 -> 결과 1행
    - Test 2 (DATA-01, D-01): 같은 고유코드라도 마켓이 다르면 별도 행이다. 입력: "ABC001"+"스마트스토어" 1행, "ABC001"+"쿠팡" 1행 -> 결과 2행
    - Test 3 (DATA-02): 주문수량이 숫자로 합산된다. 입력: "ABC001"+"스마트스토어" 수량 3, 5, 2 -> 합계 10
    - Test 4 (DATA-03, D-02): 최근 주문일 기준 상품명이 선택된다. 입력: 주문일 2026-01-01 상품명 "A제품", 주문일 2026-03-15 상품명 "B제품" -> 결과 "B제품"
    - Test 5 (DATA-04): 최근 주문일이 결과에 포함된다. 위 입력 -> 최근 주문일 2026-03-15
    - Test 6 (D-03): 결과 컬럼이 정확히 [마켓명, 판매자 고유코드, 상품명, 주문수량 합계, 최근 주문일] 이다
    - Test 7 (D-04): 주문수량이 빈 값이거나 숫자 변환 불가인 행은 제외되고 요약에 skipped_rows로 카운트된다
    - Test 8 (D-05): 판매자 고유코드 또는 마켓명이 빈 행은 제외되고 요약에 skipped_rows로 카운트된다
    - Test 9: 반환값이 (DataFrame, summary_dict) 튜플이다. summary_dict는 {"total_rows": int, "skipped_rows": int, "result_rows": int} 형태
    - Test 10 (D-06, D-07): 필수 컬럼이 누락되면 ValueError를 발생시킨다. 필수 컬럼: "마켓명", "판매자 고유코드", "상품명", "주문수량", "주문일"
  </behavior>
  <action>
    1. requirements.txt 생성:
       ```
       flask>=3.1,<3.2
       pandas>=2.2,<2.3
       openpyxl>=3.1,<3.2
       pytest>=8.0
       ```

    2. src/__init__.py, tests/__init__.py 생성 (빈 파일)

    3. tests/fixtures/sample.xlsx 생성 — openpyxl로 프로그래밍 방식으로 생성하는 헬퍼 함수를 tests/conftest.py에 작성:
       - create_test_xlsx(rows, path) 함수: 리스트 of dict를 받아 .xlsx 파일 생성
       - 컬럼명은 D-07에 따라 정확히: "고객 주문 마켓별칭", "마켓명", "주문일", "주문번호", "판매자 고유코드", "상품코드", "상품명", "옵션명", "주문수량", "주문가격", "수령자", "전화번호", "주소"
       - pytest fixture로 tmp_path에 테스트 엑셀 파일을 동적 생성

    4. tests/test_processor.py 작성 — 위 behavior의 10개 테스트 케이스:
       - from src.processor import process_sales_data
       - 각 테스트는 conftest의 create_test_xlsx로 임시 엑셀 파일 생성 후 process_sales_data(파일경로) 호출
       - 반환값 검증: (DataFrame, dict) 튜플
       - DataFrame 컬럼 검증: ["마켓명", "판매자 고유코드", "상품명", "주문수량 합계", "최근 주문일"]
       - summary dict 키 검증: total_rows, skipped_rows, result_rows

    5. src/processor.py에 스텁 함수만 생성:
       ```python
       import pandas as pd

       def process_sales_data(file_path):
           """판매 데이터를 처리하여 고유코드+마켓별 집계 결과를 반환한다."""
           raise NotImplementedError
       ```

    6. pytest 실행하여 모든 테스트가 FAIL (RED) 상태 확인
  </action>
  <verify>
    <automated>cd /Users/lucas/project/ex2 && python -m pytest tests/test_processor.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt 파일이 존재하고 "pandas>=2.2" 문자열을 포함한다
    - tests/test_processor.py 파일이 존재하고 "def test_" 패턴이 10개 이상이다
    - tests/conftest.py 파일이 존재하고 "def create_test_xlsx" 문자열을 포함한다
    - src/processor.py 파일이 존재하고 "def process_sales_data" 문자열을 포함한다
    - pytest 실행 시 10개 이상의 테스트가 FAILED 상태이다 (NotImplementedError)
    - 테스트 파일에 "마켓명" 컬럼명이 사용된다 (D-07)
    - 테스트 파일에 "판매자 고유코드" 컬럼명이 사용된다 (D-07)
  </acceptance_criteria>
  <done>10개 이상의 테스트가 모두 FAILED(RED) 상태이며, 테스트 코드가 D-01~D-07의 모든 결정사항을 검증한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: process_sales_data 구현 (GREEN) 및 리팩터링</name>
  <files>src/processor.py</files>
  <read_first>
    src/processor.py
    tests/test_processor.py
    tests/conftest.py
    .planning/phases/01-core-processing-pipeline/01-CONTEXT.md
  </read_first>
  <behavior>
    - 모든 RED 테스트가 GREEN으로 전환된다
    - process_sales_data(file_path) -> (DataFrame, summary_dict)
  </behavior>
  <action>
    src/processor.py에 process_sales_data 함수를 구현한다:

    1. 엑셀 읽기:
       ```python
       df = pd.read_excel(file_path, engine="openpyxl")
       ```

    2. 필수 컬럼 검증 (D-06, D-07):
       필수 컬럼 리스트: ["마켓명", "판매자 고유코드", "상품명", "주문수량", "주문일"]
       누락 시 ValueError("필수 컬럼이 누락되었습니다: {누락 컬럼}")

    3. total_rows = len(df) 기록

    4. 비정상 데이터 제외 (D-04, D-05):
       - 판매자 고유코드 또는 마켓명이 NaN이거나 빈 문자열인 행 제외
       - 주문수량을 pd.to_numeric(df["주문수량"], errors="coerce")로 변환, NaN인 행 제외
       - skipped_rows = total_rows - len(valid_df)

    5. 주문일 변환:
       pd.to_datetime(valid_df["주문일"], errors="coerce")

    6. 집계 (D-01, D-02, D-03):
       groupby 키: ["판매자 고유코드", "마켓명"]
       각 그룹에서:
       - "주문수량 합계": 주문수량의 sum()
       - "최근 주문일": 주문일의 max()
       - "상품명": 주문일이 max인 행의 상품명 (D-02: 최근 주문일 기준)

       구현 방식 — groupby + apply 또는 agg:
       ```python
       def aggregate_group(group):
           latest_idx = group["주문일"].idxmax()
           return pd.Series({
               "마켓명": group.name[1],
               "판매자 고유코드": group.name[0],
               "상품명": group.loc[latest_idx, "상품명"],
               "주문수량 합계": group["주문수량"].sum(),
               "최근 주문일": group["주문일"].max(),
           })

       result = valid_df.groupby(["판매자 고유코드", "마켓명"]).apply(aggregate_group).reset_index(drop=True)
       ```

    7. 결과 컬럼 순서 정렬 (D-03):
       result = result[["마켓명", "판매자 고유코드", "상품명", "주문수량 합계", "최근 주문일"]]

    8. 요약 dict 생성:
       summary = {"total_rows": total_rows, "skipped_rows": skipped_rows, "result_rows": len(result)}

    9. return (result, summary)

    10. pytest 실행하여 모든 테스트 GREEN 확인

    11. 리팩터링이 필요하면 수행 (함수 추출, 변수명 정리 등). pytest 재실행하여 GREEN 유지 확인.
  </action>
  <verify>
    <automated>cd /Users/lucas/project/ex2 && python -m pytest tests/test_processor.py -v</automated>
  </verify>
  <acceptance_criteria>
    - pytest 실행 결과 10개 이상의 테스트가 모두 PASSED이다
    - src/processor.py에 "def process_sales_data" 함수가 존재한다
    - src/processor.py에 "pd.read_excel" 호출이 있다
    - src/processor.py에 "groupby" 호출이 있다
    - src/processor.py에 "ValueError" 발생 코드가 있다 (필수 컬럼 누락 시)
    - src/processor.py에 "pd.to_numeric" 호출이 있다 (주문수량 변환)
    - src/processor.py에 "to_datetime" 호출이 있다 (주문일 변환)
    - 결과 DataFrame의 컬럼이 정확히 ["마켓명", "판매자 고유코드", "상품명", "주문수량 합계", "최근 주문일"]이다 (테스트로 검증됨)
  </acceptance_criteria>
  <done>모든 테스트가 PASSED(GREEN)이며, process_sales_data가 D-01~D-07의 모든 결정사항을 정확히 구현한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파일 입력 -> process_sales_data | 사용자가 업로드한 엑셀 파일을 pandas로 읽는 경계. 악성 파일 가능성. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | process_sales_data file_path 파라미터 | mitigate | Phase 2에서 파일 확장자/MIME 타입 검증. Phase 1에서는 필수 컬럼 검증으로 구조적 무결성 확인 (ValueError on missing columns). |
| T-01-02 | Denial of Service | 대용량 엑셀 파일 입력 | accept | 개인 로컬 도구이므로 대용량 공격 시나리오 낮음. Phase 2에서 MAX_CONTENT_LENGTH 설정으로 파일 크기 제한 가능. |
| T-01-03 | Information Disclosure | 엑셀 파일 내 민감정보 (수령자, 전화번호, 주소) | mitigate | process_sales_data는 결과에 수령자/전화번호/주소 컬럼을 포함하지 않음. 입력 데이터는 메모리에서만 처리되고 디스크에 저장하지 않음. |
| T-01-04 | Tampering | openpyxl XML 파싱 취약점 | accept | 개인 로컬 도구 + openpyxl은 활발히 유지보수됨. 최신 버전 사용으로 알려진 취약점 방지. |
</threat_model>

<verification>
1. `python -m pytest tests/test_processor.py -v` 실행 시 모든 테스트 PASSED
2. src/processor.py가 존재하고 process_sales_data 함수를 export
3. 결과 DataFrame 컬럼: 마켓명, 판매자 고유코드, 상품명, 주문수량 합계, 최근 주문일
4. 요약 dict: total_rows, skipped_rows, result_rows 키 포함
5. 비정상 데이터(빈 수량, 빈 키) 입력 시 건너뛰고 skipped_rows에 카운트
</verification>

<success_criteria>
- process_sales_data(file_path) 함수가 (DataFrame, summary_dict) 튜플을 반환한다
- 10개 이상의 단위 테스트가 모두 통과한다
- DATA-01~DATA-04 요구사항과 D-01~D-07 결정사항이 모두 구현되고 테스트로 검증된다
- Phase 2에서 이 모듈을 import하여 바로 사용할 수 있다
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-processing-pipeline/01-01-SUMMARY.md`
</output>
