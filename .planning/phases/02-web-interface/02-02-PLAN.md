---
phase: 02-web-interface
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements:
  - UPLD-02
  - DISP-01

must_haves:
  truths:
    - "브라우저에서 업로드 화면이 정상 표시된다"
    - "파일 선택 후 결과 테이블이 표시된다"
    - "드래그 앤 드롭으로 파일 업로드가 동작한다"
    - "에러 시 한국어 메시지가 표시된다"
  artifacts: []
  key_links: []
---

<objective>
브라우저에서 Flask 앱의 전체 동작을 수동 검증한다. 자동화 테스트로 확인 불가능한 시각적 UI(색상, 레이아웃, 줄무늬), JavaScript 동작(드래그앤드롭, 로딩 피드백)을 사용자가 직접 확인한다.

Purpose: UPLD-02(드래그앤드롭)와 D-02~D-05(시각적 디자인)는 브라우저에서만 검증 가능하다.
Output: 사용자 승인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-web-interface/02-UI-SPEC.md
@.planning/phases/02-web-interface/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 브라우저에서 전체 UI/UX 수동 검증</name>
  <files>none</files>
  <read_first>
    - .planning/phases/02-web-interface/02-UI-SPEC.md (시각적 기대값)
  </read_first>
  <action>
    Flask 개발 서버를 실행하고 사용자에게 브라우저 검증을 요청한다.

    1. `flask --app src.app run --debug` 실행
    2. 사용자에게 http://127.0.0.1:5000 접속을 안내
    3. 아래 검증 항목을 사용자에게 제시하고 결과를 대기

    **검증 항목:**

    (1) 업로드 화면 (D-01, D-02, D-03): 파란색 헤더 바에 "판매정보 분석기", 안내 텍스트, 파란색 "파일 선택" 버튼

    (2) 파일 선택 업로드 (UPLD-01, D-04): 버튼 클릭으로 파일 선택, "처리 중..." 로딩 피드백, 결과 화면으로 교체

    (3) 결과 화면 (DISP-01, DISP-03, D-05~D-08): 요약 텍스트, 5개 컬럼 테이블, 줄무늬, 콤마 포맷(1,234), YYYY-MM-DD 날짜, "새 파일 업로드" 버튼

    (4) 드래그 앤 드롭 (UPLD-02, D-03): 파일 드래그 시 파란색 오버레이, 드롭 시 업로드 진행

    (5) 에러 처리 (UPLD-03): 잘못된 파일 업로드 시 빨간색 한국어 에러 메시지, 업로드 영역 유지

    테스트용 파일: tests/fixtures/sample.xlsx
  </action>
  <verify>
    <automated>cd /Users/lucas/project/ex2 && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>사용자가 5개 검증 항목을 모두 확인하고 "approved"를 입력하거나 수정 사항을 제시했다.</done>
  <acceptance_criteria>
    - 사용자가 5개 검증 항목 모두 확인 완료
    - 사용자가 "approved" 입력 또는 수정 사항 제시
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No additional trust boundaries. This plan is a verification checkpoint only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | Verification-only plan, no new code |
</threat_model>

<verification>
사용자가 브라우저에서 전체 UI/UX를 확인하고 승인한다.
</verification>

<success_criteria>
1. 사용자가 모든 검증 항목을 확인하고 "approved"를 입력했다
2. 또는 수정 사항을 제시하여 해당 내용이 반영되었다
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-interface/02-02-SUMMARY.md`
</output>
